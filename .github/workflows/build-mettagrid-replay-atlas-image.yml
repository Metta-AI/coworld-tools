name: "Build MettaGrid Replay Atlas Image"

on:
  pull_request:
    paths:
      - ".github/workflows/build-mettagrid-replay-atlas-image.yml"
      - "reporters/reporters/mettagrid/mettagrid_replay_atlas/**"
      - "reporters/reporters/reporter_sdk/**"
      - "reporters/reporters/episode_analysis/**"
  push:
    branches: [main]
    paths:
      - ".github/workflows/build-mettagrid-replay-atlas-image.yml"
      - "reporters/reporters/mettagrid/mettagrid_replay_atlas/**"
      - "reporters/reporters/reporter_sdk/**"
      - "reporters/reporters/episode_analysis/**"
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: metta-ai/reporters-mettagrid-replay-atlas

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Compute tags
        id: tags
        run: |
          REPO="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}"
          SHORT_SHA="${GITHUB_SHA::12}"
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            TAGS="${REPO}:pr-${{ github.event.number }}"
          else
            TAGS="${REPO}:latest,${REPO}:main-${SHORT_SHA}"
          fi
          echo "tags=${TAGS}" >> "$GITHUB_OUTPUT"
          echo "primary_tag=${REPO}:main-${SHORT_SHA}" >> "$GITHUB_OUTPUT"

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./reporters/reporters
          file: ./reporters/reporters/mettagrid/mettagrid_replay_atlas/Dockerfile
          platforms: linux/amd64
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.tags.outputs.tags }}
