# Pins for reproducible chart output within an image build; the libraries
# themselves are pulled in by reporter_sdk / episode-analysis[charts].
pydantic>=2,<3
pyarrow>=18,<25
requests>=2.32,<3
numpy>=2,<3
matplotlib>=3.8,<4
