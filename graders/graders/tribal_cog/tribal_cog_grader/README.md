# Tribal Cog Grader

Starter Coworld grader for Tribal Cog / Tribal Fortress style episodes.

It reads `team_scores`, `winner_team`, `steps`, and `truncation_reason` from `results.json`, then scores episodes
for team separation, decisive winner presence, meaningful duration, and explicit completion signals.

Build:

```bash
./build.sh
```
